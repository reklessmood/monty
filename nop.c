#include "monty.h"

/**
 * nop - Does nothing.
 * @head: Pointer to the stack's head
 * @counter: Line number
 * Return: No return value
 */
void nop(stack_t **head, unsigned int counter)
{
	(void)counter;
	(void)head;
}
